import Phaser from 'phaser';

/**
 * Manages physics, collision, and hitbox detection for fighting mechanics.
 * Placeholder - will handle attack hitboxes, hurtboxes, and knockback.
 */
export class PhysicsManager {
  constructor(private readonly scene: Phaser.Scene) {}

  update(): void {
    // Physics/collision logic will be implemented here
  }
}
