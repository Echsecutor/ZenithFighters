import Phaser from 'phaser';

/**
 * AI controller for computer-controlled opponents.
 * Placeholder - will implement decision logic for attacks, blocks, and movement.
 */
export class AIController {
  constructor(
    private readonly scene: Phaser.Scene,
    private readonly _fighter: Phaser.GameObjects.GameObject,
  ) {}

  update(): void {
    // AI decision logic will be implemented here
  }
}
